# Python
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
myenv/
.venv/
.venv-nb/
venv/

# Jupyter
.ipynb_checkpoints/

# OS junk
.DS_Store
Thumbs.db

# Logs
*.log

# Runtime artifacts created by app/main.py
tmp/
chroma_db/

# Secrets
.env

# Internal project report - not for the public repo
docs/report.md

# Large/low-value trained model checkpoints (optimizer/scheduler state, the
# full fine-tuned weights, and raw per-step training logs) - all reproducible
# from notebooks/llm_labs.ipynb, not needed for inference. Track with Git LFS
# instead of plain git if the weight files need to be versioned.
models/full/optimizer.pt
models/full/scheduler.pt
models/full/rng_state.pth
models/full/pytorch_model.bin
models/full/trainer_state.json
models/full/training_args.bin
models/peft/optimizer.pt
models/peft/scheduler.pt
models/peft/rng_state.pth
models/peft/trainer_state.json
models/peft/training_args.bin
