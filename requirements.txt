# Full install - both the fine-tuning notebook and the RAG chatbot app.
# Only need one of the two? Install requirements-notebook.txt or
# requirements-app.txt directly instead - it's a much lighter install.
-r requirements-notebook.txt
-r requirements-app.txt
